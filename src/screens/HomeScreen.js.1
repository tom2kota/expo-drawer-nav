import React from 'react';
import { Text, StyleSheet } from 'react-native';

const HomeScreen = () => {
  const pageName = 'Home Screen';
  return <Text style={styles.textStyle}>{pageName}</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
    color: 'blue',
    padding: 30
  },
});

export default HomeScreen;
